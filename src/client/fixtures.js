import { STATUS_CODES } from "./status-codes"

export const mockedInternalServerError = {
  statusCode: STATUS_CODES.INTERNAL_SERVER_ERROR
}

export const mockedBadRequest = {
  statusCode: STATUS_CODES.BAD_REQUEST
}